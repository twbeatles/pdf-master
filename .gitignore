__pycache__/
*.pyc
*.pyo
*.pyd

# Test / analysis caches
.pytest_cache/
.pytest_tmp/
.mypy_cache/
.ruff_cache/
.pyright/
.pyrightcache/
.tmp_pyright_test*/
.hypothesis/
.tox/
.nox/
# Local pytest/agent scratch logs (cwd)
.pytest_*.txt
.collect.txt
.test_files.txt
.b1.txt
.b2.txt

# Coverage
.coverage
.coverage.*
coverage.xml
htmlcov/

# Packaging / build artifacts
*.egg-info/
.eggs/
pip-wheel-metadata/
*.whl
*.tar.gz
*.log
/build_log.txt
build/
dist/
# PyInstaller intermediate (also under build/, keep explicit)
warn-*.txt
xref-*.html
# package_smoke / local EXE leftovers outside dist (if any)
PDF_Master_v*.exe

# App/runtime temp files
backup/
# Atomic I/O temps (worker_runtime/io.py, settings save) — any extension
.pdf_master_*.tmp*
.pdf_master_*
# AI encrypted-PDF unlock temps (ai_ops / temp_cleanup; may appear under cwd if tempdir mis-set)
pdf_master_ai_*
pdf_master_ai_*.pdf
pdf_master_undo/
# Local settings corruption backups (also under ~/, keep cwd accident pattern)
.pdf_master_settings.json
.pdf_master_settings.json.backup_*

# Virtual environments
.venv/
venv/
__pypackages__/
.pyre/
.pytype/

# Merge / patch leftovers
*.orig
*.rej

# Local dev / agent tooling
.codegraph/
.grok/
mcps/
terminals/
agent-tools/
.sessions/
*.session.md
# Spec Kit / agent ephemeral at **repo root only**
# (do NOT use bare `specs/` — it also matched `docs/superpowers/specs/` and hid design docs)
.specify/
/specs/

# Optional design notes under docs/ are tracked; ignore only local worktrees
docs/**/worktrees/

# Local one-off analysis dumps occasionally created in cwd
*.pyright.txt
pyright-out*.txt
.pytest_last_failed.txt
# pytest / agent capture dumps written to repo root (not under .pytest_tmp/)
/pytest_out.txt
/pytest_full.txt
/pytest_*.txt
# Occasional local verification dumps
/pyright_*.txt
# Ephemeral agent/session scratch (never commit)
.claude/
.cursor/
.codex/
.agent/
.agents/
.superpowers/
tmp/
temp/

# OS / editor files
Thumbs.db
Desktop.ini
.DS_Store
.idea/
.vscode/
*.swp
*~
*.bak
